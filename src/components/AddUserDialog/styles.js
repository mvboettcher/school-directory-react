const styles = (theme) => ({
  form: {
    width: '100%'
  },
  sexFormControl: {
    minWidth: 140
  },
  typeFormControl: {
    minWidth: 140,
    marginLeft: 30
  },
  selectEmpty: {
    marginTop: theme.spacing(2)
  }
})

export default styles
