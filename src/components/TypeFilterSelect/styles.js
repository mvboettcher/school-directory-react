const styles = (theme) => ({
  form: {
    width: '100%'
  },
  sexFormControl: {
    minWidth: 140,
    marginTop: 16,
    marginBottom: 8
  },
  typeFormControl: {
    minWidth: 140,
    marginLeft: 30,
    marginTop: 16,
    marginBottom: 8
  },
  selectEmpty: {
    marginTop: theme.spacing(2)
  },
  selectEmpty: {
    marginTop: theme.spacing(2)
  }
})

export default styles
