const styles = (theme) => ({
  appContainer: {
    height: '100%',
    padding: 20
  },
  header: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  },
  searchFilter: {
    display: 'flex',
    justifyContent: 'space-evenly',
    paddingTop: 20,
    paddingBottom: 20
  }
})

export default styles
