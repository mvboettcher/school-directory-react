const styles = (theme) => ({
  appContainer: {
    height: '100%',
    padding: 20
  },
  header: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }
})

export default styles
