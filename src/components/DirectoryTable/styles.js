const styles = (theme) => ({
  table: {
    minWidth: 650
  },
  editDeleteContainer: {
    display: 'flex',
    justifyContent: 'space-evenly'
  },
  editDeleteButton: {
    opacity: 0.7,
    cursor: 'pointer'
  },
  progress: {
    marginTop: 20
  }
})

export default styles
